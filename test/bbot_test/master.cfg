# -*- python -*-
# ex: set syntax=python:

#
# This file is essentially boilerplate.  You should be able to use
# this master.cfg (with your project name replacing "bbot_test")
# directly.
#
import os, sys, lazy_reload

lazy_reload.lazy_reload('bbot_test')

# When buildbot checkconfig is invoked from the root directory of the
# project (as opposed to its parent), we need to amend the path so
# bbot_test can be found.
try:
    import bbot_test
except ImportError:
    sys.path[-1] = os.path.dirname(sys.path[-1])

# All the real work gets done in bbot_test.config
from bbot_test.config import *
